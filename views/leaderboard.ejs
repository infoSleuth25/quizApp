<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Leaderboard</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* Base styles for the entire page */
        body {
            background-color: #6D7A8A;
            font-family: 'Poppins', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
        }

        /* Styling for the leaderboard container */
        .leaderboard-container {
            width: 80%;
            background-color: #FFDAB9;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            border: 3px solid black;
        }

        /* Styling for the main heading */
        h1 {
            font-size: 2.5rem;
            color: #2C3E50;
            margin-bottom: 20px;
        }

        /* Table styling */
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        /* Table header */
        thead th {
            background-color: #151111;
            color: white;
            padding: 15px;
            font-size: 1.2rem;
            font-weight: bold;
            text-align: left;
        }

        /* Table rows */
        tbody td {
            background-color: #FFF;
            padding: 15px;
            border-bottom: 1px solid #ddd;
            font-size: 1rem;
            text-align: left;
            color: #333;
        }

        /* Hover effect for table rows */
        tbody tr:hover {
            background-color: #f1f1f1;
        }

        /* Back button styling */
        .back-button {
            background-color: #151111;
            color: white;
            padding: 10px 20px;
            font-size: 1.2rem;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            display: inline-block;
        }

        .back-button:hover {
            background-color: #6D7A8A;
            border: 2px solid black;
        }

        /* Responsive layout for smaller screens */
        @media (max-width: 768px) {
            .leaderboard-container {
                width: 95%;
                padding: 20px;
            }

            table, thead th, tbody td {
                font-size: 0.9rem;
            }

            h1 {
                font-size: 2rem;
            }

            .back-button {
                font-size: 1rem;
                padding: 8px 16px;
            }
        }
    </style>
</head>
<body>
    <div class="leaderboard-container">
        <h1>Quiz Leaderboard</h1>
        <table>
            <thead>
                <tr>
                    <th>Student Name</th>
                    <th>Score</th>
                    <th>Total Score</th>
                    <th>Date Taken</th>
                </tr>
            </thead>
            <tbody>
                <% results.forEach(result => { %>
                <tr>
                    <td><%= result.student.studentFirstName %> <%= result.student.studentLastName %></td>
                    <td><%= result.score %></td>
                    <td><%= result.totalScore %></td>
                    <td><%= result.dateTaken.toDateString() %></td>
                </tr>
                <% }) %>
            </tbody>
        </table>
        <a href="/teacher/<%= username %>/pastQuizzes" class="back-button">Back to Quizzes</a>
    </div>
</body>
</html>
